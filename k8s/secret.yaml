apiVersion: v1
kind: Secret
metadata:
  name: yogadaily-bot-secret
  labels:
    app: yogadaily-bot
type: Opaque
data:
  # Токен Telegram бота (закодированный в base64)
  # Пример: echo -n "your_bot_token_here" | base64
  BOT_TOKEN: "REPLACE_WITH_BASE64_ENCODED_TOKEN"
